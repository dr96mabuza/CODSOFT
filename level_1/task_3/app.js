const calculator = () => {
    /**
     * Addition function takes only two whole numbers and return the sum.
     * @param {Number} num1 
     * @param {Number} num2 
     */
    const addition = (num1, num2) => {
        return Number(num1) + Number(num2);
    }

    /**
     * 
     * @param {Number} num1 
     * @param {Number} num2 
     */
    const subtraction = (num1, num2) => {}
    /**
     * 
     * @param {Number} num1 
     * @param {Number} num2 
     */
    const division = (num1, num2) => {}

    /**
     * 
     * @param {Number} num1 
     * @param {Number} num2 
     */
    const multiplication = (num1, num2) => {}

    return {addition, multiplication, addition, subtraction}
}